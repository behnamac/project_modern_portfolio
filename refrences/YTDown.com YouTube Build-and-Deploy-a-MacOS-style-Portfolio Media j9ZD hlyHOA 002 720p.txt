(Transcribed by TurboScribe. Go Unlimited to remove this message.)

Stop building boring portfolios. No, seriously. If your portfolio still starts with Hi, I'm blank and there's an image on the right, we need to talk. 

Because the world really doesn't need another polite portfolio quietly whispering, Here are my projects, please consider me. That version of you is invisible. Recruiters scroll. 

They forget. They move on to the next developer who copy-pasted a template and vibe-coded a personality-free website. Zero creativity. 

Zero identity. Zero impact. But that's not you. 

Not today. Hi there, I'm Adrian and welcome to the portfolio of the 21st century. A portfolio so bold, so interactive, so unignorable that the first time someone sees it, they'll swear their laptop just booted a brand new operating system.

Hold up, wait a minute. Wait, what? Yes, your new portfolio is a macOS-inspired desktop environment. A portfolio that doesn't look like a website. 

It behaves like an actual OS. And here's what you're about to build. A fully interactive macOS-like desktop.

Smooth animated welcome experience. A magnifying dock with real physics. A full Finder clone. 

A Safari window for your blog. A photos app. A contact app.

A terminal for displaying your tech stack. A resume viewer with a download button. True-to-life draggable windows.

And Gsap-powered animations that feel premium and alive. Let that sink in. A desktop. 

Apps. Animations. You're building an entire operating system that doubles as your portfolio.

And this isn't just about being flashy. It's about becoming the kind of developer who actually stands out. Because while building this, you'll learn React fundamentals and advanced patterns.

Tailwind CSS. Zestand. Immer. 

Gsap. Higher-order components. And clean UI engineering principles that separate juniors from seniors.

By the end, you won't just have a portfolio. You'll have a signature piece. A career weapon, if you will.

And if you want to go even further and master advanced Gsap patterns and real product-level motion, the Ultimate Web Animation course goes deep into everything I use here. Or if you want to skip the setup and jump straight into building, I prepared a complete starter kit for you. Not a templated clone.

A real, extendable foundation that goes beyond just what this course teaches. It includes a dark mode, a mobile interface, modular components, app systems, and a professional readme. And if you want to go beyond just looking good and actually get interviews, I've prepared a hiring-ready resume, case study templates, and step-by-step guides to make your LinkedIn and GitHub stand out.

It's how you'll present yourself like a real candidate in a crowded market. Grab it from the description below. It'll save you weeks and give you a system you can grow with.

Alright, you know the features, you know the skills, and the tools. You know exactly what you're about to build. So whether you're following along from scratch or you use the starter kit, what matters is you walk away with something that feels yours.

Something unforgettable. Something that finally shows the world what you're capable of. Let's build your new portfolio.

And since my goal is to make sure you land a job, this portfolio is not just a regular project. It's a career asset designed to showcase everything you're capable of, making you an attractive candidate for any job. See, hosting your website on a personalized domain makes you more professional and improves your credibility.

It also highlights your skill in self-hosting, scaling, and managing websites. So before we dive into the code, we need to set the foundation. I highly recommend Hostinger for hosting this app.

And they're offering an amazing deal right now because there's a huge Black Friday sale going on. So click the Hostinger link in the description and then click start now. When it comes to hosting this app, I'll personally go with the premium plan.

It allows you to host three websites with the same account. It comes with 20 gigs of SSD storage. You get a free domain for one year and free SSL for HTTPS security, which builds trust with anybody visiting your site.

This deal won't last forever. And because I partnered with Hostinger, you'll get an even bigger discount. So click the link in the description, start now, and choose a plan.

Here, we have to choose the period of our hosting. And with the amazing deal going on right now, I'll choose 48 months to save the most. If you already know the domain name you want to get with the hosting, you can type it out right here, or you can do that later.

And most importantly, here's that additional coupon code that I promised. Just type in javascriptmastery and you'll get an even bigger discount on your hosting package. You can now continue and very soon we'll start developing our app.

And as soon as you buy it, you'll be redirected to Hostinger's guided setup. In this case, we're creating a new website. We're not using any kind of a builder.

Rather, we want to start with an empty HTML website. And if you bought 12 months or more, you've also gotten a free domain name. In this case, choose what would suit you.

I'll go with something like mymacosportfolio.com. I'll leave better domain names for you and then click next. Or if you don't yet know which domain name you want to choose, you can use a temporary domain name for now. But I recommend just putting a combination of your last and first name.

And that's it. Our domain is getting registered. It'll ask you where your target audience is located.

You can choose anything here. I'll go with United States. And while our hosting is being set up, I just wanted to quickly let you know that if you want to skip all the trial and error while trying to build out this portfolio, you can get the full source code below.

Just fix up your details and then still follow along with the video, but this time trying to better understand how the code base really works. Now that our hosting has been set up, at the end of the video, we'll simply deploy our code using the file manager. And before we dive right into the code, one quick thing.

Since my goal is to help you not just build projects, but actually grow your developer career, I want to remind you why I built JSMastery Pro in the first place. It's designed to help smart and ambitious developers like you stay consistent with your learning, get real-world results, and finally feel confident applying for jobs. And if you've been thinking about joining, now's the best time as our Black Friday sale goes live on the 25th.

Every course, bundle, and membership will be 25% off and that stacks on top of the already huge PPP discount. But heads up, the deal only lasts until December 2nd. So mark your calendar.

All right, now let's really dive into the code. Ready to make your portfolio? Let's get started. First, open up your code editor or IDE and create a new folder called macOS Portfolio.

Throughout the course, you'll see me use WebStorm. It is a professional IDE that as of recently became completely free for non-commercial use. I'll put the link down in the description so you can download it.

And since I know you'll be asking which theme I'm using, it's Cat Poochin' Mocha. Now WebStorm also comes paired with Juni, a smart coding agent. Throughout this course, we'll use it a couple of times to help us develop some pieces of the UI.

It is super useful and saves a lot of time. You can immediately get it by pressing Command Shift P, heading over to Plugins, and then searching for Juni. So now that we've repaired our coding environment, we are ready to get started.

I'll open up the integrated terminal. You can do that by pressing Control and then the key left of the one key on your keyboard. And within it, you can type the command of npm create vt add latest and then add the dot slash, which means that it'll create it within the current repository.

And then press Enter. It'll ask you whether you want to install create vt installer. You can say Y for yes.

And then if it says your current directory is not empty, you can just say empty it and let's continue. Now it'll ask you a couple of questions on how we want to set up our vt environment. We'll be using React to build this project, but you're more than welcome to follow along with any framework that you like.

In this case, I'll go with React. And just for simplicity's sake, I'll use plain JavaScript. But once again, you're free to use TypeScript as well.

Now it asks us whether we want to use rolldown-vt. I'll skip it for now because it's still experimental. But by the time you're watching this video, it could already be added by default.

Finally, are we ready to install the packages and start? I'll say yes. It should take just a couple of seconds for everything to get installed. And we'll be running on localhost 5173.

So you can open it up within your browser. And it should look something like this. Now, as you can see, I have no top bar or anything else cluttering the view.

And that's because I'm using Arc. It's a nice browser designed as a Chrome replacement. And this is not sponsored, but I really like using it.

So now you know. Now, instead of seeing this empty boilerplate, what do you say that we clean up our project a bit to prepare it for our application? I'll head back over to my IDE, and we can explore the code base together. Starting from top to bottom, we have the vconfig, which is pretty much empty right now.

Then there's the readme explaining what our project is all about. A package.json, which goes over all of our dependencies. Index.html, which acts as the starting point of our application.

Later on right here, we'll replace this base favicon image, as well as the title, to make it personalized to your portfolio. Then there's the eslint config, gitignore, and what we care most about are the public and the source folders. Within the public folder, you'll have all of the images and assets that we'll use across our entire application.

For now, I'll go ahead and remove it, as later on we'll recreate it and add so many more assets. And then finally, there's the source folder, which also contains some assets, which we can remove from now. And we also have a couple of other files that we can go over together.

The main file simply takes our entire application and adds it to this index.html file. Then we have the index.css, which we'll modify later on. There's the app.jsx, which acts as the starting point of our application.

So I want to remove everything from here, as it currently has only this boilerplate. Instead, I want to run rafce. This will spin up a simple React functional component that looks something like this.

If this didn't work for you, you might need to head over to plugins and install modern React snippets. Then after you restart your IDE, it should work. Finally, there's the app.css, which we'll remove from now, as we'll do all of our styling within index, as well as within our files directly, as we'll be using Tailwind throughout this project.

So if within your app.jsx, you render an h1 that says something like welcome to my Macfolio and save it, back in the browser, you should be able to see something that looks like this. It's not much, but we're getting there. And this is a great point to put my browsers side by side with my editor, so we can see what we're doing live.

Now, as promised, we'll use Tailwind CSS. It's a utility-first CSS framework, packed with classes like flex, padding-top for text center, and so on, which makes it super easy to style your applications directly within your JSX, with a simple syntax still allowing for complete customizability. To install it, you can simply search for Vite, and you can see how to get started using Vite.

First, we create a new project, which we've already done. And then the second step is to install Tailwind CSS and add Tailwind CSS Vite. So I'll open up my terminal again.

Actually, I will keep the first one running with my application, and I'll call it app. And then I'll rename the second one to terminal, within which we can run additional commands. And I'll install Tailwind and add Tailwind CSS forward slash Vite.

Once that is done, the next step is to configure the Vite plugin within our Vite configuration. So if you head right here within the Vite config, right here next to React, you can just add Tailwind CSS coming from add Tailwind CSS Vite. Then the next step is to import Tailwind CSS within our index.css file.

You can do that by heading over to index.css, removing everything we have over there, and just say add import Tailwind CSS. Finally, you can rerun your application, and then start using Tailwind by adding classes like text-3xl, font-bold, and underline, which I will add directly to my h1 within the app. So I'll say class, but it has to be class name because we're in React, text-3xl, font-bold, and underline.

And as you can see, we have a large heading that is bold and underlined, which means that we've successfully set up Tailwind. Now, alongside this build, I've also prepared a complete Figma design containing the full design of everything we'll build, showing you how different windows will look like, even giving you choice of different wallpapers that you can add as the background of our macOS portfolio. Now, obviously, this is a highly customized build, which will contain a lot of custom icons and fonts.

So to save you some time from clicking around Figma and trying to download each one of these icons individually, I've took the time to collect them all for you in a single, easy-to-download zip. It is a part of the video kit that you can find in the description of this video. So once you head over there, you can click on it, and here you can click Assets.

Just go ahead and download it. Once it's there, simply unzip it, and then drag and drop it to the root of your application. It should look something like this.

You can see that there's a folder to upload your resume. There's also all of the icons which we'll be using, the images, a MacBook for the favicon, and that's it. Immediately, this will allow you to head back over to index.html, replace this link from vit.svg to macbook.png. This will change this favicon right here on the left, and then you can also modify the title to be your name and then portfolio.

That way, when you open up your sidebar, you'll see your portfolio. And alongside the assets right below, you'll also be able to find a complete index.css. So you can copy it and paste it right here. It'll seem like a lot, but basically what we're doing here is importing a font we'll be using, importing Tailwind CSS as before, making some modifications to the HTML in the body, setting up the theme, and then creating all the different utility classes that we'll be using throughout this project.

For example, styling how the main will look like, the nav bar, and so on. So at any point in time, you'll be able to head back to this index.css to see exactly how we're styling things. So if you head back to your portfolio right now, you should already be able to see something that looks like this.

Seems like we already have done, but that is definitely not the case. The only thing you're seeing here is a wallpaper. Now is the time that we take that and turn it into something more like this, with a full nav bar where you can open up and see info about your different projects, including your blog posts or maybe a terminal that shows the tech stack that you're good at.

A contact me section, as well as the finder where you can browse through all of your works. All of that is coming soon. You can follow along and learn how to build it with me in this course.

Or if you want to skip ahead and make this project your own faster, I've made the complete source code available below. You'll still be able to follow along and build everything for free, but if you want to save time or dive deeper, that's an option. My team and I spent more than a month designing and developing this portfolio to make it super unique.

So I wanted to give you an option to get it right away and make it your own. With that in mind, we can continue with the rest of the setup. And the one part of the setup is heading back to our VConfig and adding support for absolute imports.

You know, so you don't have to say dot dot slash dot dot slash components slash something. Rather, we can just do something like add components forward slash component name. This is much better.

And to make that happen within our code base, you have to import resolve from path as well as their name. And you also have to import file URL to path. And this is coming from URL.

Then right below our plugins, you can say your resolve. We'll be resolving aliases for different files and folders. And you can make it an object where we can define either an at sign or a hash sign.

And I'll first do it for the components where I'll resolve it to their name. File URL to path. And then you need to import the URL and then define a comma and the name.

In this case, it's going to be the components folder. You can now duplicate this a few times. I'll do it one, two, three, four more times.

For the second one, we'll do it for constants. Make sure to change the name as well as the name right here. Then the third time we'll do it for the store.

This is going to be the global data store of our application. Then we'll have it HOC for higher order components. Yep, that's coming up soon as well.

And finally, we'll do it for different windows that are going to appear within our macOS system. And then to make it work, you can also add a new file called jsconfig. Within here, you can define an object with compiler options, within which you're going to define a base URL of your application, which is going to be just the dot, meaning current repository.

And we are in a JSON file, so you have to use double-quoted strings. Then we'll define the paths and set them as hash anything, which will start from source. Like this.

The paths, of course, will be inside of an object, so we can put this part right here within it. Finally, we can go below compiler options and say include, and we want to include the source directory. This was that most tedious part that we had to go through, where now we have set up our file and folder structure as well as our styling to be able to accommodate and make it easier for us to implement the rest of this amazing macOS portfolio.

So in the next lesson, let's start from top to bottom and implement the navbar. To get started developing our navbar, let's create a new folder within the source folder and call it components. Within components, create a new file and call it navbar.jsx, and within it, run rafce.

Once you have it there, you can then import it within the app.jsx. The only thing you have to do is wrap everything in a main tag, because this is the main part of our content, and then right here, you can render navbar. You can see that right now we get an error saying that the file doesn't seem to exist, so most likely it's an issue with our vconfig, where we're looking for it at components, but actually it's coming from source components. So at the start of all of these paths right here at the end, you also need to add source and then forward slash a specific file.

That'll look like this, components coming from source components, constants coming from source constants, and so on. And that way, it'll able to auto import the navbar. And you can see that we can see it right there.

And then you can just command click into the navbar, and we can start developing it. First things first, I'll make it an HTML5 semantic nav tag, within which I'll have a div. Within that div, I will render an image with a source of forward slash images forward slash logo.svg. If you save that, you'll automatically see that we'll get this nice background and an Apple logo at the top left.

Now, if you're wondering how these styles got applied, well, the answer lies in the index.css. If you head over here, you'll see that automatically to the nav element, we're applying a property of flex, justify between item center, as well as this bg-white-50. If I remove that, you'll see that it is gone. I'm also applying a bit of a background blur, so we get this nice shadow at the bottom, as well as a padding of two, padding horizontal of five, and that's it.

At any point, if you're wondering which styles are applied, you can just head over to the index.css and find the corresponding element. Now, an image also needs an alt tag, so I'll say logo, and right below it, I'll render a p tag that will have a class name of font-bold, and it'll say Adrian's Portfolio. Or for you, it's not going to say Adrian's, it's going to say your name.

Now, below this p tag, I'll also render a ul, an unordered list. They'll need to render a collection of different links that'll look something like this. It'll be an array where inside of it, we'll have one object for each link, and it'll contain an ID, something like one, and then the name, maybe like portfolio.

Then we'll have a second one and a third one, so it'll have IDs of two and three, and names will be something like contact and projects. So now we have this array with these elements, and we can also map over them. So I'll say dot map, where we get each individual item, and then for each item, we want to automatically return an li.

Now, when I say automatically return, what do I mean by that? Well, I mean that right here, we're not using a curly brace, but rather just a parenthesis, because if you use the curly brace, that will mean that you're opening up a function block, and in that case, you would have to add an explicit return statement right here. But if you just make it a parenthesis, that means that it'll auto return whatever is within it. Then to this li, we can give a key equal to ID, and then within the li, we'll render a P that'll simply render the name of the link.

Of course, that name is coming from the item, so we can destructure both the ID and the name from the item by using this object destructuring syntax, putting it within the curly braces, allowing you to just extract the properties within it. And now we can see that we get this portfolio contact and projects right here at the top. But there's one improvement that we can make here.

I never like to have data right here within our presentation. JSX and components like the navbar are for presenting something to the user, but this right here is just raw data. So what I typically like to do is extract it.

I'll first extract this array by copying it, and then I'll define it right here at the top by saying cons data is equal to, and then we can render it right here. So now the presentation part looks much better. You simply say data.map and render what needs to be rendered.

But there's an even better way to do it. Instead of keeping this data here, I will copy it and create a new folder within a source folder called constants. Within constants, you can create a new file called index.js. I'm using index because that's the starting point of every folder.

Within it, you can simply paste what you copied, this data, and I will rename it to nav links. And then you can simply say export const nav links, or you can declare it right here and then export it at the bottom like this. That way, we now have a special file called constants within which we can declare all of our data pieces.

The only thing you have to do now to use it is just import it by saying nav links. You can see that WebStorm will automatically recognize that it's coming from constants and take a look. That's it.

We can still see them, but the presentation is much more beautiful. Now, talking about constants, they'll contain absolutely all the data that will be specific to you as this is your portfolio. Within the video kit down below, I'll provide a complete constants index.js file, so you can copy it right now and paste it right here.

It'll seem like there's so much stuff happening right here, but trust me, not really. If we collapse some of these large pieces of data, you'll see that this is the place where you'll actually define your projects. Here is your work, and then within the work, you can define the name, the icon, the position, and most importantly, the additional description for each one of the projects that you develop.

That means that this portfolio doesn't just work for me and my projects. You can just come in here and change the data very easily, just like changing a JSON object. If you scroll to the top, you'll see that the first thing that we have are the actual nav links.

We also have the nav icons here, which are going to be useful for rendering those icons. So let's head back, and we're still importing the same thing from the same file. And whenever the file is index.js, specifically for that file name, you don't even have to define it.

The first thing that our program will look into when looking at the constants folder is going to be the index.js. Now, below this div containing the left portion of our nav bar, we'll create another div that'll be all about the right side. Within it, we'll also have a UL, and within that UL, we will map over nav icons this time, dot map, where we're going to get each individual icon, and we can automatically return an li for it. Each li will have a key equal to icon.id. But instead of saying icon every time, we can destructure the most important properties from the icon, such as the id and the image, img.

And now we can just say key is equal to id, and within the li, I'll render an image tag with a source of img, a class name equal to icon-hover, and I'll also give it an alt tag equal to a template string of icon- and then id. That way, all alt tags are unique. And now you can see some icons appear at the top right.

Now, we're not replicating a full macOS operating system, so these won't be functional. But if you want to get the final source code, in there, I also included a light and dark mode switch. So you can either get it from there, or maybe that's going to be a great challenge for the end of this course.

Finally, I also want to display the current date and time, as every operating system has that. For that, we'll use a day.js library. So the only thing you have to do is head over to your terminal and run npm i day.js. Once you install it, you can simply import it at the top by saying import day.js, coming from day.js. I always like to space out my external imports at the top, and then have my internal imports be right below them.

And then right here at the bottom, below this ul, still on the right side div, I'll render a time component. Yep, if you didn't know, there is an HTML5 component for it.

(This file is longer than 30 minutes. Go Unlimited at https://turboscribe.ai/ to transcribe files up to 10 hours long.)